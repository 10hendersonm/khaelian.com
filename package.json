{
  "name": "khaelian.com",
  "version": "0.0.0",
  "scripts": {
    "start": "npx lerna run start --stream",
    "build": "npx lerna run build",
    "install:production": "npx lerna bootstrap -- --prod",
    "clean": "npm run clean:packages && npm run clean:root",
    "clean:packages": "npx lerna exec -- rm -rf node_modules",
    "clean:root": "rm -rf node_modules"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {},
  "dependencies": {
    "lerna": "3.22.1"
  }
}
